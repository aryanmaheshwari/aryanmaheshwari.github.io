/**
 * 
 * TODO: 
 * 
 * Generic component
 * 
 * will always:
 * 1. have rounded edges
 * 2. be width of the div it's in
 * 3. be height of the div it's in
 * 
 * mutable:
 * 1. input type (will be specified in props)
 * 2. minimum length
 * 
 * error checking:
 * 1. signal invalid input
 *   A) too short
 *   B) incorrect format
 * 
 * REQUIRED: TAKE IN ONCLICK FUNCTION FrOM WHERE CALLED (ex. in case submit form, save user input, navigate)
 */
